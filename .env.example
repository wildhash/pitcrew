# Archestra API Configuration
# Mode: "mock" (default) or "real"
ARCHESTRA_MODE=mock

# Base URL for Archestra API (required when ARCHESTRA_MODE=real)
# Example: http://localhost:3000 or https://archestra.example.com
ARCHESTRA_API_URL=http://localhost:3000

# Request timeout in milliseconds (default: 8000)
ARCHESTRA_TIMEOUT_MS=8000
